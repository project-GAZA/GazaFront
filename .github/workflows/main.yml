name: deploy

on:
  push:
    branches:
      - dev
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: SSH-deploy
        uses: appleboy/ssh-action@master
        with:
          host: 175.45.194.207
          username: root
          password: rootgaza
          port: 22
          script: |
            whoami
            cd GazaFront
            pwd
            nvm use 21
            nvm ls
            git pull origin dev
            pm2 kill
            netstat -lntp
            pm2 start "yarn dev" --watch
            netstat -lntp
